/*

let getPrice = function(hours, isUrgent) {
  
  let rate = 1500;
  
  if (isUrgent) {
    hours /= 2;
    rate *= 2.5;
  }
  
  if (hours > 150) {
    rate -= 250;  
  }
  
  let price = rate * hours;
  
  return price;
}

getPrice(300, true);

console.log(getPrice(300, true));*/